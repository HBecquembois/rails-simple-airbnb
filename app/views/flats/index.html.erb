<h1>All the flats</h1>
<h4>There are <%= Flat.count %> flats available</h4>

<div class="container">
  <div class="flats-cards-container">
    <% @flats.each do |flat| %>
      <div class="flat-card d-flex ">
        <h3><%= flat.name %></h3>
        <h5><%= flat.address %></h5>
        <h5><%= flat.price_per_night %></h5>
        <h5><%= flat.number_of_guests %></h5>
        <%= link_to "More info", flat %>
        <%= link_to "Edit", edit_flat_path(flat.id) %>
        <%= link_to "Remove", flat_path(flat.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
      </div>
    <% end %>
  </div>
</div>
